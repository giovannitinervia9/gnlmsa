% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gnlmsa_methods.R
\name{fitted.gnlmsa}
\alias{fitted.gnlmsa}
\title{Extract Fitted Values from a Generalized Non-Linear Model}
\usage{
\method{fitted}{gnlmsa}(object, type = c("mu", "phi", "variance", "all"), ...)
}
\arguments{
\item{object}{An object of class \code{"gnlmsa"}, typically returned by \code{\link[=gnlmsa]{gnlmsa()}}.}

\item{type}{Character string specifying the type of fitted values to return.
Must be one of \code{"mu"} (default), \code{"phi"}, \code{"variance"}, or \code{"all"}.}

\item{...}{Further arguments passed to or from other methods (currently unused).}
}
\value{
Depending on \code{type}:
\itemize{
\item A numeric vector of fitted values (\code{"mu"}, \code{"phi"}, or \code{"variance"}).
\item A data frame containing all three components if \code{type = "all"}.
}
}
\description{
Returns fitted values from a fitted Generalized Non-Linear Model (GNLM) object of class \code{"gnlmsa"},
including the mean component, the dispersion component, and the variance function evaluated at the fitted values.
}
\details{
Available options for \code{type} are:
\itemize{
\item \code{"mu"}: Fitted values for the mean component \eqn{\hat{\mu}}.
\item \code{"phi"}: Fitted values for the dispersion component \eqn{\hat{\phi}}.
\item \code{"variance"}: Variance evaluated at the fitted mean and dispersion, using the \code{variance()} function
defined by the family.
\item \code{"all"}: A data frame containing \code{mu}, \code{phi}, and \code{variance}.
}
}
