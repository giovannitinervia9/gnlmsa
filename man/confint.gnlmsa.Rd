% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gnlmsa_methods.R
\name{confint.gnlmsa}
\alias{confint.gnlmsa}
\title{Confidence Intervals for Generalized Non-Linear Model Parameters}
\usage{
\method{confint}{gnlmsa}(
  object,
  parm,
  level = 0.95,
  test = c("Wald", "Rao", "LRT"),
  expected = TRUE,
  ...
)
}
\arguments{
\item{object}{An object of class \code{"gnlmsa"} returned by \code{\link[=gnlmsa]{gnlmsa()}}.}

\item{parm}{Parameters for which confidence intervals are required. Currently ignored; confidence intervals are computed for all parameters.}

\item{level}{Confidence level required. Default is \code{0.95} for 95\% confidence intervals.}

\item{test}{Character string specifying the type of test to use. Currently, only \code{"Wald"} is supported.
If a different test is specified, a warning is issued and the function falls back to \code{"Wald"}.}

\item{expected}{Logical. If \code{TRUE} (default), uses the expected Fisher information to compute standard errors;
otherwise uses the observed information.}

\item{...}{Further arguments passed to or from other methods (currently unused).}
}
\value{
A data frame with two columns, giving the lower and upper bounds of the confidence intervals
for each parameter.
}
\description{
Computes confidence intervals for the parameters of a fitted Generalized Non-Linear Model (GNLM) object
of class \code{"gnlmsa"}, based on the variance-covariance matrix of the parameter estimates.
}
\details{
Currently, only Wald-type confidence intervals are implemented.

Confidence intervals are computed on the transformed (unconstrained) scale and then mapped back
to the original (possibly constrained) parameter space.

The variance-covariance matrix used in the computation accounts for the parameter transformations,
via the Jacobian of the transformation at the point estimates.
}
